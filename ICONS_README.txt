图标文件说明
============

Chrome 扩展需要以下图标文件（可选，但建议添加）：
- icon16.png (16x16 像素)
- icon48.png (48x48 像素)  
- icon128.png (128x128 像素)

如果没有这些文件，Chrome 会使用默认图标。

创建图标的方法：
1. 使用在线图标生成器（如 favicon.io）
2. 使用图像编辑软件（如 Photoshop、GIMP）
3. 从图标库下载（如 flaticon.com）

建议使用与指纹识别相关的图标，如：
- 放大镜
- 指纹图案
- 检测/扫描图标

